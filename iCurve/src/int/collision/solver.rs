use alloc::vec::Vec;
use crate::int::collision::collider::Collider;

pub struct Solver {
    colliders_0: Vec<Collider>,
    colliders_1: Vec<Collider>
}

impl Solver {
    fn intersect(sp0: Collider, sp1: Collider) {

    }
}